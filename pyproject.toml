[project]
name = "email-triage-openenv"
version = "1.0.0"
description = "AI-powered email triage environment using OpenEnv"
authors = [
    { name="Anil Kumar" }
]
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "requests"
]

[tool.openenv]
entrypoint = "server.app:app"
